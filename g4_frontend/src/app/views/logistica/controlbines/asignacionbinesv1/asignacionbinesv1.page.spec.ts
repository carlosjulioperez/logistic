import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Asignacionbinesv1Page } from './asignacionbinesv1.page';

describe('Asignacionbinesv1Page', () => {
  let component: Asignacionbinesv1Page;
  let fixture: ComponentFixture<Asignacionbinesv1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Asignacionbinesv1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
