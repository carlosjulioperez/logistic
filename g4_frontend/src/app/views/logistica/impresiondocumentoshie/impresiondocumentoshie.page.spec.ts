import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ImpresiondocumentoshiePage } from './impresiondocumentoshie.page';

describe('ImpresiondocumentoshiePage', () => {
  let component: ImpresiondocumentoshiePage;
  let fixture: ComponentFixture<ImpresiondocumentoshiePage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ImpresiondocumentoshiePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
